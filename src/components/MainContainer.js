import React from 'react';
import Form from './Form';
import Table from './Table';

const MainContainer = () => {
    return (
        <div>
            <Form />
            <Table />
        </div>
    );
};

export default MainContainer;